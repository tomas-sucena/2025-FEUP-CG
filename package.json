{
  "name": "cg-2025-t04-g06",
  "devDependencies": {
    "live-server": "^1.2.2",
    "prettier": "3.5.2"
  },
  "scripts": {
    "format": "npx prettier . --write",
    "tp1": "npx live-server --open=tp1/",
    "tp2": "npx live-server --open=tp2/",
    "tp3": "npx live-server --open=tp3/",
    "tp4": "npx live-server --open=tp4/"
  }
}
